version: "1.1"
name: sum
env:
  baseUrl: http://localhost:8080
tests:
  sum:
    steps:
      - name: sum
        http:
          url: ${{env.baseUrl}}/api/sum
          params:
            input1: 1
            input2: 2
          method: GET
          check:
            status: 200
            jsonpath:
              $.sum: 3
